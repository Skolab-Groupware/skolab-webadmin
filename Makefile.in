# Makefile.in generated by automake 1.9.5 from Makefile.am.
# @configure_input@

# Copyright (C) 1994, 1995, 1996, 1997, 1998, 1999, 2000, 2001, 2002,
# 2003, 2004, 2005  Free Software Foundation, Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.

@SET_MAKE@


srcdir = @srcdir@
top_srcdir = @top_srcdir@
VPATH = @srcdir@
pkgdatadir = $(datadir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkgincludedir = $(includedir)/@PACKAGE@
top_builddir = .
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
INSTALL = @INSTALL@
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
subdir = .
DIST_COMMON = README $(am__configure_deps) $(noinst_HEADERS) \
	$(srcdir)/Makefile.am $(srcdir)/Makefile.in \
	$(srcdir)/kolab-webadmin.spec.in $(top_srcdir)/configure \
	AUTHORS COPYING ChangeLog INSTALL NEWS install-sh missing
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/configure.ac
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
am__CONFIG_DISTCLEAN_FILES = config.status config.cache config.log \
 configure.lineno configure.status.lineno
mkinstalldirs = $(install_sh) -d
CONFIG_CLEAN_FILES = kolab-webadmin.spec
SOURCES =
DIST_SOURCES =
am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
am__vpath_adj = case $$p in \
    $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
    *) f=$$p;; \
  esac;
am__strip_dir = `echo $$p | sed -e 's|^.*/||'`;
am__installdirs = "$(DESTDIR)$(phpincludesdir)" \
	"$(DESTDIR)$(phplocalededir)" "$(DESTDIR)$(phplocalefrdir)" \
	"$(DESTDIR)$(phplocalenldir)" "$(DESTDIR)$(phptemplatesdir)" \
	"$(DESTDIR)$(wsaddressbookdir)" "$(DESTDIR)$(wsadmindir)" \
	"$(DESTDIR)$(wsadminstratordir)" \
	"$(DESTDIR)$(wsdistributionlistdir)" "$(DESTDIR)$(wskolabdir)" \
	"$(DESTDIR)$(wsmaintainerdir)" "$(DESTDIR)$(wspicsdir)" \
	"$(DESTDIR)$(wsservicedir)" "$(DESTDIR)$(wssharedfolderdir)" \
	"$(DESTDIR)$(wstopleveldir)" "$(DESTDIR)$(wsuserdir)"
phpincludesDATA_INSTALL = $(INSTALL_DATA)
phplocaledeDATA_INSTALL = $(INSTALL_DATA)
phplocalefrDATA_INSTALL = $(INSTALL_DATA)
phplocalenlDATA_INSTALL = $(INSTALL_DATA)
phptemplatesDATA_INSTALL = $(INSTALL_DATA)
wsaddressbookDATA_INSTALL = $(INSTALL_DATA)
wsadminDATA_INSTALL = $(INSTALL_DATA)
wsadminstratorDATA_INSTALL = $(INSTALL_DATA)
wsdistributionlistDATA_INSTALL = $(INSTALL_DATA)
wskolabDATA_INSTALL = $(INSTALL_DATA)
wsmaintainerDATA_INSTALL = $(INSTALL_DATA)
wspicsDATA_INSTALL = $(INSTALL_DATA)
wsserviceDATA_INSTALL = $(INSTALL_DATA)
wssharedfolderDATA_INSTALL = $(INSTALL_DATA)
wstoplevelDATA_INSTALL = $(INSTALL_DATA)
wsuserDATA_INSTALL = $(INSTALL_DATA)
DATA = $(phpincludes_DATA) $(phplocalede_DATA) $(phplocalefr_DATA) \
	$(phplocalenl_DATA) $(phptemplates_DATA) $(wsaddressbook_DATA) \
	$(wsadmin_DATA) $(wsadminstrator_DATA) \
	$(wsdistributionlist_DATA) $(wskolab_DATA) \
	$(wsmaintainer_DATA) $(wspics_DATA) $(wsservice_DATA) \
	$(wssharedfolder_DATA) $(wstoplevel_DATA) $(wsuser_DATA)
HEADERS = $(noinst_HEADERS)
ETAGS = etags
CTAGS = ctags
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
distdir = $(PACKAGE)-$(VERSION)
top_distdir = $(distdir)
am__remove_distdir = \
  { test ! -d $(distdir) \
    || { find $(distdir) -type d ! -perm -200 -exec chmod u+w {} ';' \
         && rm -fr $(distdir); }; }
DIST_ARCHIVES = $(distdir).tar.gz $(distdir).tar.bz2
GZIP_ENV = --best
distuninstallcheck_listfiles = find . -type f -print
distcleancheck_listfiles = find . -type f -print
ACLOCAL = @ACLOCAL@
AMTAR = @AMTAR@
AUTOCONF = @AUTOCONF@
AUTOHEADER = @AUTOHEADER@
AUTOMAKE = @AUTOMAKE@
AWK = @AWK@
CYGPATH_W = @CYGPATH_W@
DEFS = @DEFS@
ECHO_C = @ECHO_C@
ECHO_N = @ECHO_N@
ECHO_T = @ECHO_T@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_SCRIPT = @INSTALL_SCRIPT@
INSTALL_STRIP_PROGRAM = @INSTALL_STRIP_PROGRAM@
LIBOBJS = @LIBOBJS@
LIBS = @LIBS@
LTLIBOBJS = @LTLIBOBJS@
MAKEINFO = @MAKEINFO@
PACKAGE = @PACKAGE@
PACKAGE_BUGREPORT = @PACKAGE_BUGREPORT@
PACKAGE_NAME = @PACKAGE_NAME@
PACKAGE_STRING = @PACKAGE_STRING@
PACKAGE_TARNAME = @PACKAGE_TARNAME@
PACKAGE_VERSION = @PACKAGE_VERSION@
PATH_SEPARATOR = @PATH_SEPARATOR@
SET_MAKE = @SET_MAKE@
SHELL = @SHELL@
STRIP = @STRIP@
VERSION = @VERSION@
ac_ct_STRIP = @ac_ct_STRIP@
am__leading_dot = @am__leading_dot@
am__tar = @am__tar@
am__untar = @am__untar@
bindir = @bindir@
build_alias = @build_alias@
datadir = @datadir@
exec_prefix = @exec_prefix@
host_alias = @host_alias@
includedir = @includedir@
infodir = @infodir@
install_sh = @install_sh@
libdir = @libdir@
libexecdir = @libexecdir@
localstatedir = @localstatedir@
mandir = @mandir@
mkdir_p = @mkdir_p@
oldincludedir = @oldincludedir@
phpdir = @phpdir@
pkgdocdir = @pkgdocdir@
prefix = @prefix@
program_transform_name = @program_transform_name@
sbindir = @sbindir@
sharedstatedir = @sharedstatedir@
spec_build_date = @spec_build_date@
sysconfdir = @sysconfdir@
target_alias = @target_alias@
wsdocrootdir = @wsdocrootdir@
EXTRA_DIST = php/admin/include/config.php.in $(WSTOPLEVEL_FILES) \
	$(WSADMIN_FILES) $(WSADDRESSBOOK_FILES) \
	$(WSADMINISTRATOR_FILES) $(WSDISTRIBUTIONLIST_FILES) \
	$(WSKOLAB_FILES) $(WSMAINTAINER_FILES) $(WSPICS_FILES) \
	$(WSSERVICE_FILES) $(WSsharedfolder_FILES) $(WSUSER_FILES) \
	$(PHP_INCLUDES) $(PHP_TEMPLATES) $(PHP_LOCALE_DE_PO) \
	$(PHP_LOCALE_FR_PO) $(PHP_LOCALE_NL_PO)
CLEANFILES = php/admin/include/config.php
do_subst = sed \
	-e 's,[@]prefix[@],@prefix@,g' \
	-e 's,[@]phpdir[@],$(phpdir),g' \
	-e 's,[@]localstatedir[@],$(localstatedir),g'

noinst_HEADERS = @PACKAGE@.spec
WSTOPLEVEL_FILES = www/favicon.ico
wstopleveldir = $(wsdocrootdir)
wstoplevel_DATA = $(WSTOPLEVEL_FILES)
WSADMIN_FILES = www/admin/index.php \
	www/admin/logout.php \
	www/admin/style.css 

wsadmindir = $(wsdocrootdir)/admin
wsadmin_DATA = $(WSADMIN_FILES)
WSADDRESSBOOK_FILES = www/admin/addressbook/addr.php \
	www/admin/addressbook/index.php

wsaddressbookdir = $(wsadmindir)/addressbook
wsaddressbook_DATA = $(WSADDRESSBOOK_FILES)
WSADMINISTRATOR_FILES = www/admin/administrator/admin.php \
	www/admin/administrator/index.php

wsadminstratordir = $(wsadmindir)/administrator
wsadminstrator_DATA = $(WSADMINISTRATOR_FILES)
WSDISTRIBUTIONLIST_FILES = www/admin/distributionlist/index.php \
	www/admin/distributionlist/list.php

wsdistributionlistdir = $(wsadmindir)/distributionlist
wsdistributionlist_DATA = $(WSDISTRIBUTIONLIST_FILES)
WSKOLAB_FILES = www/admin/kolab/erfrakon.php \
	www/admin/kolab/index.php \
	www/admin/kolab/versions.php \
	www/admin/kolab/intevation.php \
	www/admin/kolab/codefusion.php \
	www/admin/kolab/kdab.php \
	www/admin/kolab/kde.php

wskolabdir = $(wsadmindir)/kolab
wskolab_DATA = $(WSKOLAB_FILES)
WSMAINTAINER_FILES = www/admin/maintainer/maintainer.php \
	www/admin/maintainer/index.php

wsmaintainerdir = $(wsadmindir)/maintainer
wsmaintainer_DATA = $(WSMAINTAINER_FILES)
WSPICS_FILES = www/admin/pics/erfrakon.png \
	www/admin/pics/codefusion.png \
	www/admin/pics/intevation_logo.png \
	www/admin/pics/kdab.png \
	www/admin/pics/kde.jpg \
	www/admin/pics/kolab_logo.png

wspicsdir = $(wsadmindir)/pics
wspics_DATA = $(WSPICS_FILES)
WSSERVICE_FILES = www/admin/service/index.php
wsservicedir = $(wsadmindir)/service
wsservice_DATA = $(WSSERVICE_FILES)
WSsharedfolder_FILES = www/admin/sharedfolder/sf.php \
	www/admin/sharedfolder/index.php

wssharedfolderdir = $(wsadmindir)/sharedfolder
wssharedfolder_DATA = $(WSsharedfolder_FILES)
WSUSER_FILES = www/admin/user/forward.php \
	www/admin/user/index.php \
	www/admin/user/user.php \
	www/admin/user/vacation.php \
	www/admin/user/deliver.php

wsuserdir = $(wsadmindir)/user
wsuser_DATA = $(WSUSER_FILES)
phpadmindir = $(phpdir)/admin
PHP_INCLUDES = php/admin/include/config.php \
	php/admin/include/menu.php \
	php/admin/include/debug.php \
	php/admin/include/form.class.php \
	php/admin/include/auth.class.php \
	php/admin/include/ldap.class.php \
	php/admin/include/sieveutils.class.php \
	php/admin/include/mysmarty.php \
	php/admin/include/headers.php \
	php/admin/include/authenticate.php \
	php/admin/include/locale.php \
	php/admin/include/Sieve.php

phpincludesdir = $(phpadmindir)/include
phpincludes_DATA = $(PHP_INCLUDES)
PHP_TEMPLATES = php/admin/templates/maintainerdeleted.tpl \
	php/admin/templates/kde.tpl \
	php/admin/templates/addrdeleted.tpl \
	php/admin/templates/userdeleted.tpl \
	php/admin/templates/page.tpl \
	php/admin/templates/addresslist.tpl \
	php/admin/templates/sflistall.tpl \
	php/admin/templates/erfrakon.tpl \
	php/admin/templates/codefusion.tpl \
	php/admin/templates/adminlistall.tpl \
	php/admin/templates/login.tpl \
	php/admin/templates/maintainerlistall.tpl \
	php/admin/templates/kolab.tpl \
	php/admin/templates/versions.tpl \
	php/admin/templates/addrlistall.tpl \
	php/admin/templates/forward.tpl \
	php/admin/templates/deliver.tpl \
	php/admin/templates/distlistall.tpl \
	php/admin/templates/kdab.tpl \
	php/admin/templates/userlistall.tpl \
	php/admin/templates/service.tpl \
	php/admin/templates/welcome.tpl \
	php/admin/templates/systemaliasnagscreen.tpl \
	php/admin/templates/intevation.tpl \
	php/admin/templates/vacation.tpl \
	php/admin/templates/createuser.tpl \
	php/admin/templates/formcontainer.tpl \
	php/admin/templates/admindeleted.tpl

phptemplatesdir = $(phpadmindir)/templates
phptemplates_DATA = $(PHP_TEMPLATES)

### i18n stuff
# gettextize didn't make much sense for a php project
# so we install the .mo files "manually"
# PENDING(steffen): Improve this
MSGFMT = msgfmt
PHP_LOCALE_DE_PO = php/admin/locale/de/LC_MESSAGES/messages.po
PHP_LOCALE_DE_MO = php/admin/locale/de/LC_MESSAGES/messages.mo
PHP_LOCALE_FR_PO = php/admin/locale/fr/LC_MESSAGES/messages.po
PHP_LOCALE_FR_MO = php/admin/locale/fr/LC_MESSAGES/messages.mo
PHP_LOCALE_NL_PO = php/admin/locale/nl/LC_MESSAGES/messages.po
PHP_LOCALE_NL_MO = php/admin/locale/nl/LC_MESSAGES/messages.mo
localedir = $(phpadmindir)/locale
phplocalededir = $(localedir)/de/LC_MESSAGES
phplocalefrdir = $(localedir)/fr/LC_MESSAGES
phplocalenldir = $(localedir)/nl/LC_MESSAGES
phplocalede_DATA = $(PHP_LOCALE_DE_MO)
phplocalefr_DATA = $(PHP_LOCALE_FR_MO)
phplocalenl_DATA = $(PHP_LOCALE_NL_MO)
all: all-am

.SUFFIXES:
.SUFFIXES: .mo .po
am--refresh:
	@:
$(srcdir)/Makefile.in:  $(srcdir)/Makefile.am  $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      echo ' cd $(srcdir) && $(AUTOMAKE) --gnu '; \
	      cd $(srcdir) && $(AUTOMAKE) --gnu  \
		&& exit 0; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --gnu  Makefile'; \
	cd $(top_srcdir) && \
	  $(AUTOMAKE) --gnu  Makefile
.PRECIOUS: Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    echo ' $(SHELL) ./config.status'; \
	    $(SHELL) ./config.status;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $@ $(am__depfiles_maybe)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $@ $(am__depfiles_maybe);; \
	esac;

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	$(SHELL) ./config.status --recheck

$(top_srcdir)/configure:  $(am__configure_deps)
	cd $(srcdir) && $(AUTOCONF)
$(ACLOCAL_M4):  $(am__aclocal_m4_deps)
	cd $(srcdir) && $(ACLOCAL) $(ACLOCAL_AMFLAGS)
kolab-webadmin.spec: $(top_builddir)/config.status $(srcdir)/kolab-webadmin.spec.in
	cd $(top_builddir) && $(SHELL) ./config.status $@
uninstall-info-am:
install-phpincludesDATA: $(phpincludes_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(phpincludesdir)" || $(mkdir_p) "$(DESTDIR)$(phpincludesdir)"
	@list='$(phpincludes_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(phpincludesDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(phpincludesdir)/$$f'"; \
	  $(phpincludesDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(phpincludesdir)/$$f"; \
	done

uninstall-phpincludesDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(phpincludes_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(phpincludesdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(phpincludesdir)/$$f"; \
	done
install-phplocaledeDATA: $(phplocalede_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(phplocalededir)" || $(mkdir_p) "$(DESTDIR)$(phplocalededir)"
	@list='$(phplocalede_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(phplocaledeDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(phplocalededir)/$$f'"; \
	  $(phplocaledeDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(phplocalededir)/$$f"; \
	done

uninstall-phplocaledeDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(phplocalede_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(phplocalededir)/$$f'"; \
	  rm -f "$(DESTDIR)$(phplocalededir)/$$f"; \
	done
install-phplocalefrDATA: $(phplocalefr_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(phplocalefrdir)" || $(mkdir_p) "$(DESTDIR)$(phplocalefrdir)"
	@list='$(phplocalefr_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(phplocalefrDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(phplocalefrdir)/$$f'"; \
	  $(phplocalefrDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(phplocalefrdir)/$$f"; \
	done

uninstall-phplocalefrDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(phplocalefr_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(phplocalefrdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(phplocalefrdir)/$$f"; \
	done
install-phplocalenlDATA: $(phplocalenl_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(phplocalenldir)" || $(mkdir_p) "$(DESTDIR)$(phplocalenldir)"
	@list='$(phplocalenl_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(phplocalenlDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(phplocalenldir)/$$f'"; \
	  $(phplocalenlDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(phplocalenldir)/$$f"; \
	done

uninstall-phplocalenlDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(phplocalenl_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(phplocalenldir)/$$f'"; \
	  rm -f "$(DESTDIR)$(phplocalenldir)/$$f"; \
	done
install-phptemplatesDATA: $(phptemplates_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(phptemplatesdir)" || $(mkdir_p) "$(DESTDIR)$(phptemplatesdir)"
	@list='$(phptemplates_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(phptemplatesDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(phptemplatesdir)/$$f'"; \
	  $(phptemplatesDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(phptemplatesdir)/$$f"; \
	done

uninstall-phptemplatesDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(phptemplates_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(phptemplatesdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(phptemplatesdir)/$$f"; \
	done
install-wsaddressbookDATA: $(wsaddressbook_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(wsaddressbookdir)" || $(mkdir_p) "$(DESTDIR)$(wsaddressbookdir)"
	@list='$(wsaddressbook_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(wsaddressbookDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(wsaddressbookdir)/$$f'"; \
	  $(wsaddressbookDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(wsaddressbookdir)/$$f"; \
	done

uninstall-wsaddressbookDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(wsaddressbook_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(wsaddressbookdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(wsaddressbookdir)/$$f"; \
	done
install-wsadminDATA: $(wsadmin_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(wsadmindir)" || $(mkdir_p) "$(DESTDIR)$(wsadmindir)"
	@list='$(wsadmin_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(wsadminDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(wsadmindir)/$$f'"; \
	  $(wsadminDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(wsadmindir)/$$f"; \
	done

uninstall-wsadminDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(wsadmin_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(wsadmindir)/$$f'"; \
	  rm -f "$(DESTDIR)$(wsadmindir)/$$f"; \
	done
install-wsadminstratorDATA: $(wsadminstrator_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(wsadminstratordir)" || $(mkdir_p) "$(DESTDIR)$(wsadminstratordir)"
	@list='$(wsadminstrator_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(wsadminstratorDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(wsadminstratordir)/$$f'"; \
	  $(wsadminstratorDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(wsadminstratordir)/$$f"; \
	done

uninstall-wsadminstratorDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(wsadminstrator_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(wsadminstratordir)/$$f'"; \
	  rm -f "$(DESTDIR)$(wsadminstratordir)/$$f"; \
	done
install-wsdistributionlistDATA: $(wsdistributionlist_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(wsdistributionlistdir)" || $(mkdir_p) "$(DESTDIR)$(wsdistributionlistdir)"
	@list='$(wsdistributionlist_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(wsdistributionlistDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(wsdistributionlistdir)/$$f'"; \
	  $(wsdistributionlistDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(wsdistributionlistdir)/$$f"; \
	done

uninstall-wsdistributionlistDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(wsdistributionlist_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(wsdistributionlistdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(wsdistributionlistdir)/$$f"; \
	done
install-wskolabDATA: $(wskolab_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(wskolabdir)" || $(mkdir_p) "$(DESTDIR)$(wskolabdir)"
	@list='$(wskolab_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(wskolabDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(wskolabdir)/$$f'"; \
	  $(wskolabDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(wskolabdir)/$$f"; \
	done

uninstall-wskolabDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(wskolab_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(wskolabdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(wskolabdir)/$$f"; \
	done
install-wsmaintainerDATA: $(wsmaintainer_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(wsmaintainerdir)" || $(mkdir_p) "$(DESTDIR)$(wsmaintainerdir)"
	@list='$(wsmaintainer_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(wsmaintainerDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(wsmaintainerdir)/$$f'"; \
	  $(wsmaintainerDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(wsmaintainerdir)/$$f"; \
	done

uninstall-wsmaintainerDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(wsmaintainer_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(wsmaintainerdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(wsmaintainerdir)/$$f"; \
	done
install-wspicsDATA: $(wspics_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(wspicsdir)" || $(mkdir_p) "$(DESTDIR)$(wspicsdir)"
	@list='$(wspics_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(wspicsDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(wspicsdir)/$$f'"; \
	  $(wspicsDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(wspicsdir)/$$f"; \
	done

uninstall-wspicsDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(wspics_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(wspicsdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(wspicsdir)/$$f"; \
	done
install-wsserviceDATA: $(wsservice_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(wsservicedir)" || $(mkdir_p) "$(DESTDIR)$(wsservicedir)"
	@list='$(wsservice_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(wsserviceDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(wsservicedir)/$$f'"; \
	  $(wsserviceDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(wsservicedir)/$$f"; \
	done

uninstall-wsserviceDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(wsservice_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(wsservicedir)/$$f'"; \
	  rm -f "$(DESTDIR)$(wsservicedir)/$$f"; \
	done
install-wssharedfolderDATA: $(wssharedfolder_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(wssharedfolderdir)" || $(mkdir_p) "$(DESTDIR)$(wssharedfolderdir)"
	@list='$(wssharedfolder_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(wssharedfolderDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(wssharedfolderdir)/$$f'"; \
	  $(wssharedfolderDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(wssharedfolderdir)/$$f"; \
	done

uninstall-wssharedfolderDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(wssharedfolder_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(wssharedfolderdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(wssharedfolderdir)/$$f"; \
	done
install-wstoplevelDATA: $(wstoplevel_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(wstopleveldir)" || $(mkdir_p) "$(DESTDIR)$(wstopleveldir)"
	@list='$(wstoplevel_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(wstoplevelDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(wstopleveldir)/$$f'"; \
	  $(wstoplevelDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(wstopleveldir)/$$f"; \
	done

uninstall-wstoplevelDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(wstoplevel_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(wstopleveldir)/$$f'"; \
	  rm -f "$(DESTDIR)$(wstopleveldir)/$$f"; \
	done
install-wsuserDATA: $(wsuser_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(wsuserdir)" || $(mkdir_p) "$(DESTDIR)$(wsuserdir)"
	@list='$(wsuser_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(wsuserDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(wsuserdir)/$$f'"; \
	  $(wsuserDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(wsuserdir)/$$f"; \
	done

uninstall-wsuserDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(wsuser_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(wsuserdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(wsuserdir)/$$f"; \
	done

ID: $(HEADERS) $(SOURCES) $(LISP) $(TAGS_FILES)
	list='$(SOURCES) $(HEADERS) $(LISP) $(TAGS_FILES)'; \
	unique=`for i in $$list; do \
	    if test -f "$$i"; then echo $$i; else echo $(srcdir)/$$i; fi; \
	  done | \
	  $(AWK) '    { files[$$0] = 1; } \
	       END { for (i in files) print i; }'`; \
	mkid -fID $$unique
tags: TAGS

TAGS:  $(HEADERS) $(SOURCES)  $(TAGS_DEPENDENCIES) \
		$(TAGS_FILES) $(LISP)
	tags=; \
	here=`pwd`; \
	list='$(SOURCES) $(HEADERS)  $(LISP) $(TAGS_FILES)'; \
	unique=`for i in $$list; do \
	    if test -f "$$i"; then echo $$i; else echo $(srcdir)/$$i; fi; \
	  done | \
	  $(AWK) '    { files[$$0] = 1; } \
	       END { for (i in files) print i; }'`; \
	if test -z "$(ETAGS_ARGS)$$tags$$unique"; then :; else \
	  test -n "$$unique" || unique=$$empty_fix; \
	  $(ETAGS) $(ETAGSFLAGS) $(AM_ETAGSFLAGS) $(ETAGS_ARGS) \
	    $$tags $$unique; \
	fi
ctags: CTAGS
CTAGS:  $(HEADERS) $(SOURCES)  $(TAGS_DEPENDENCIES) \
		$(TAGS_FILES) $(LISP)
	tags=; \
	here=`pwd`; \
	list='$(SOURCES) $(HEADERS)  $(LISP) $(TAGS_FILES)'; \
	unique=`for i in $$list; do \
	    if test -f "$$i"; then echo $$i; else echo $(srcdir)/$$i; fi; \
	  done | \
	  $(AWK) '    { files[$$0] = 1; } \
	       END { for (i in files) print i; }'`; \
	test -z "$(CTAGS_ARGS)$$tags$$unique" \
	  || $(CTAGS) $(CTAGSFLAGS) $(AM_CTAGSFLAGS) $(CTAGS_ARGS) \
	     $$tags $$unique

GTAGS:
	here=`$(am__cd) $(top_builddir) && pwd` \
	  && cd $(top_srcdir) \
	  && gtags -i $(GTAGS_ARGS) $$here

distclean-tags:
	-rm -f TAGS ID GTAGS GRTAGS GSYMS GPATH tags

distdir: $(DISTFILES)
	$(am__remove_distdir)
	mkdir $(distdir)
	$(mkdir_p) $(distdir)/. $(distdir)/php/admin/include $(distdir)/php/admin/locale/de/LC_MESSAGES $(distdir)/php/admin/locale/fr/LC_MESSAGES $(distdir)/php/admin/locale/nl/LC_MESSAGES $(distdir)/php/admin/templates $(distdir)/www $(distdir)/www/admin $(distdir)/www/admin/addressbook $(distdir)/www/admin/administrator $(distdir)/www/admin/distributionlist $(distdir)/www/admin/kolab $(distdir)/www/admin/maintainer $(distdir)/www/admin/pics $(distdir)/www/admin/service $(distdir)/www/admin/sharedfolder $(distdir)/www/admin/user
	@srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's|.|.|g'`; \
	list='$(DISTFILES)'; for file in $$list; do \
	  case $$file in \
	    $(srcdir)/*) file=`echo "$$file" | sed "s|^$$srcdirstrip/||"`;; \
	    $(top_srcdir)/*) file=`echo "$$file" | sed "s|^$$topsrcdirstrip/|$(top_builddir)/|"`;; \
	  esac; \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  dir=`echo "$$file" | sed -e 's,/[^/]*$$,,'`; \
	  if test "$$dir" != "$$file" && test "$$dir" != "."; then \
	    dir="/$$dir"; \
	    $(mkdir_p) "$(distdir)$$dir"; \
	  else \
	    dir=''; \
	  fi; \
	  if test -d $$d/$$file; then \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -pR $(srcdir)/$$file $(distdir)$$dir || exit 1; \
	    fi; \
	    cp -pR $$d/$$file $(distdir)$$dir || exit 1; \
	  else \
	    test -f $(distdir)/$$file \
	    || cp -p $$d/$$file $(distdir)/$$file \
	    || exit 1; \
	  fi; \
	done
	-find $(distdir) -type d ! -perm -777 -exec chmod a+rwx {} \; -o \
	  ! -type d ! -perm -444 -links 1 -exec chmod a+r {} \; -o \
	  ! -type d ! -perm -400 -exec chmod a+r {} \; -o \
	  ! -type d ! -perm -444 -exec $(SHELL) $(install_sh) -c -m a+r {} {} \; \
	|| chmod -R a+r $(distdir)
dist-gzip: distdir
	tardir=$(distdir) && $(am__tar) | GZIP=$(GZIP_ENV) gzip -c >$(distdir).tar.gz
	$(am__remove_distdir)
dist-bzip2: distdir
	tardir=$(distdir) && $(am__tar) | bzip2 -9 -c >$(distdir).tar.bz2
	$(am__remove_distdir)

dist-tarZ: distdir
	tardir=$(distdir) && $(am__tar) | compress -c >$(distdir).tar.Z
	$(am__remove_distdir)

dist-shar: distdir
	shar $(distdir) | GZIP=$(GZIP_ENV) gzip -c >$(distdir).shar.gz
	$(am__remove_distdir)

dist-zip: distdir
	-rm -f $(distdir).zip
	zip -rq $(distdir).zip $(distdir)
	$(am__remove_distdir)

dist dist-all: distdir
	tardir=$(distdir) && $(am__tar) | GZIP=$(GZIP_ENV) gzip -c >$(distdir).tar.gz
	tardir=$(distdir) && $(am__tar) | bzip2 -9 -c >$(distdir).tar.bz2
	$(am__remove_distdir)

# This target untars the dist file and tries a VPATH configuration.  Then
# it guarantees that the distribution is self-contained by making another
# tarfile.
distcheck: dist
	case '$(DIST_ARCHIVES)' in \
	*.tar.gz*) \
	  GZIP=$(GZIP_ENV) gunzip -c $(distdir).tar.gz | $(am__untar) ;;\
	*.tar.bz2*) \
	  bunzip2 -c $(distdir).tar.bz2 | $(am__untar) ;;\
	*.tar.Z*) \
	  uncompress -c $(distdir).tar.Z | $(am__untar) ;;\
	*.shar.gz*) \
	  GZIP=$(GZIP_ENV) gunzip -c $(distdir).shar.gz | unshar ;;\
	*.zip*) \
	  unzip $(distdir).zip ;;\
	esac
	chmod -R a-w $(distdir); chmod a+w $(distdir)
	mkdir $(distdir)/_build
	mkdir $(distdir)/_inst
	chmod a-w $(distdir)
	dc_install_base=`$(am__cd) $(distdir)/_inst && pwd | sed -e 's,^[^:\\/]:[\\/],/,'` \
	  && dc_destdir="$${TMPDIR-/tmp}/am-dc-$$$$/" \
	  && cd $(distdir)/_build \
	  && ../configure --srcdir=.. --prefix="$$dc_install_base" \
	    $(DISTCHECK_CONFIGURE_FLAGS) \
	  && $(MAKE) $(AM_MAKEFLAGS) \
	  && $(MAKE) $(AM_MAKEFLAGS) dvi \
	  && $(MAKE) $(AM_MAKEFLAGS) check \
	  && $(MAKE) $(AM_MAKEFLAGS) install \
	  && $(MAKE) $(AM_MAKEFLAGS) installcheck \
	  && $(MAKE) $(AM_MAKEFLAGS) uninstall \
	  && $(MAKE) $(AM_MAKEFLAGS) distuninstallcheck_dir="$$dc_install_base" \
	        distuninstallcheck \
	  && chmod -R a-w "$$dc_install_base" \
	  && ({ \
	       (cd ../.. && umask 077 && mkdir "$$dc_destdir") \
	       && $(MAKE) $(AM_MAKEFLAGS) DESTDIR="$$dc_destdir" install \
	       && $(MAKE) $(AM_MAKEFLAGS) DESTDIR="$$dc_destdir" uninstall \
	       && $(MAKE) $(AM_MAKEFLAGS) DESTDIR="$$dc_destdir" \
	            distuninstallcheck_dir="$$dc_destdir" distuninstallcheck; \
	      } || { rm -rf "$$dc_destdir"; exit 1; }) \
	  && rm -rf "$$dc_destdir" \
	  && $(MAKE) $(AM_MAKEFLAGS) dist \
	  && rm -rf $(DIST_ARCHIVES) \
	  && $(MAKE) $(AM_MAKEFLAGS) distcleancheck
	$(am__remove_distdir)
	@(echo "$(distdir) archives ready for distribution: "; \
	  list='$(DIST_ARCHIVES)'; for i in $$list; do echo $$i; done) | \
	  sed -e '1{h;s/./=/g;p;x;}' -e '$${p;x;}'
distuninstallcheck:
	@cd $(distuninstallcheck_dir) \
	&& test `$(distuninstallcheck_listfiles) | wc -l` -le 1 \
	   || { echo "ERROR: files left after uninstall:" ; \
	        if test -n "$(DESTDIR)"; then \
	          echo "  (check DESTDIR support)"; \
	        fi ; \
	        $(distuninstallcheck_listfiles) ; \
	        exit 1; } >&2
distcleancheck: distclean
	@if test '$(srcdir)' = . ; then \
	  echo "ERROR: distcleancheck can only run from a VPATH build" ; \
	  exit 1 ; \
	fi
	@test `$(distcleancheck_listfiles) | wc -l` -eq 0 \
	  || { echo "ERROR: files left in build directory after distclean:" ; \
	       $(distcleancheck_listfiles) ; \
	       exit 1; } >&2
check-am: all-am
check: check-am
all-am: Makefile $(DATA) $(HEADERS)
installdirs:
	for dir in "$(DESTDIR)$(phpincludesdir)" "$(DESTDIR)$(phplocalededir)" "$(DESTDIR)$(phplocalefrdir)" "$(DESTDIR)$(phplocalenldir)" "$(DESTDIR)$(phptemplatesdir)" "$(DESTDIR)$(wsaddressbookdir)" "$(DESTDIR)$(wsadmindir)" "$(DESTDIR)$(wsadminstratordir)" "$(DESTDIR)$(wsdistributionlistdir)" "$(DESTDIR)$(wskolabdir)" "$(DESTDIR)$(wsmaintainerdir)" "$(DESTDIR)$(wspicsdir)" "$(DESTDIR)$(wsservicedir)" "$(DESTDIR)$(wssharedfolderdir)" "$(DESTDIR)$(wstopleveldir)" "$(DESTDIR)$(wsuserdir)"; do \
	  test -z "$$dir" || $(mkdir_p) "$$dir"; \
	done
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	$(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	  install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	  `test -z '$(STRIP)' || \
	    echo "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'"` install
mostlyclean-generic:

clean-generic:
	-test -z "$(CLEANFILES)" || rm -f $(CLEANFILES)

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
clean: clean-am

clean-am: clean-generic mostlyclean-am

distclean: distclean-am
	-rm -f $(am__CONFIG_DISTCLEAN_FILES)
	-rm -f Makefile
distclean-am: clean-am distclean-generic distclean-tags

dvi: dvi-am

dvi-am:

html: html-am

info: info-am

info-am:

install-data-am: install-phpincludesDATA install-phplocaledeDATA \
	install-phplocalefrDATA install-phplocalenlDATA \
	install-phptemplatesDATA install-wsaddressbookDATA \
	install-wsadminDATA install-wsadminstratorDATA \
	install-wsdistributionlistDATA install-wskolabDATA \
	install-wsmaintainerDATA install-wspicsDATA \
	install-wsserviceDATA install-wssharedfolderDATA \
	install-wstoplevelDATA install-wsuserDATA
	@$(NORMAL_INSTALL)
	$(MAKE) $(AM_MAKEFLAGS) install-data-hook

install-exec-am:

install-info: install-info-am

install-man:

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -f $(am__CONFIG_DISTCLEAN_FILES)
	-rm -rf $(top_srcdir)/autom4te.cache
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-generic

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-info-am uninstall-phpincludesDATA \
	uninstall-phplocaledeDATA uninstall-phplocalefrDATA \
	uninstall-phplocalenlDATA uninstall-phptemplatesDATA \
	uninstall-wsaddressbookDATA uninstall-wsadminDATA \
	uninstall-wsadminstratorDATA uninstall-wsdistributionlistDATA \
	uninstall-wskolabDATA uninstall-wsmaintainerDATA \
	uninstall-wspicsDATA uninstall-wsserviceDATA \
	uninstall-wssharedfolderDATA uninstall-wstoplevelDATA \
	uninstall-wsuserDATA

.PHONY: CTAGS GTAGS all all-am am--refresh check check-am clean \
	clean-generic ctags dist dist-all dist-bzip2 dist-gzip \
	dist-shar dist-tarZ dist-zip distcheck distclean \
	distclean-generic distclean-tags distcleancheck distdir \
	distuninstallcheck dvi dvi-am html html-am info info-am \
	install install-am install-data install-data-am \
	install-data-hook install-exec install-exec-am install-info \
	install-info-am install-man install-phpincludesDATA \
	install-phplocaledeDATA install-phplocalefrDATA \
	install-phplocalenlDATA install-phptemplatesDATA install-strip \
	install-wsaddressbookDATA install-wsadminDATA \
	install-wsadminstratorDATA install-wsdistributionlistDATA \
	install-wskolabDATA install-wsmaintainerDATA \
	install-wspicsDATA install-wsserviceDATA \
	install-wssharedfolderDATA install-wstoplevelDATA \
	install-wsuserDATA installcheck installcheck-am installdirs \
	maintainer-clean maintainer-clean-generic mostlyclean \
	mostlyclean-generic pdf pdf-am ps ps-am tags uninstall \
	uninstall-am uninstall-info-am uninstall-phpincludesDATA \
	uninstall-phplocaledeDATA uninstall-phplocalefrDATA \
	uninstall-phplocalenlDATA uninstall-phptemplatesDATA \
	uninstall-wsaddressbookDATA uninstall-wsadminDATA \
	uninstall-wsadminstratorDATA uninstall-wsdistributionlistDATA \
	uninstall-wskolabDATA uninstall-wsmaintainerDATA \
	uninstall-wspicsDATA uninstall-wsserviceDATA \
	uninstall-wssharedfolderDATA uninstall-wstoplevelDATA \
	uninstall-wsuserDATA


@distribution@

php/admin/include/config.php: Makefile php/admin/include/config.php.in
	$(mkinstalldirs) php/admin/include
	$(do_subst) <$(srcdir)/php/admin/include/config.php.in >$@

.po.mo:
	$(MSGFMT) -o $@ $<

install-data-hook:
	$(mkinstalldirs) $(DESTDIR)$(phpadmindir)/templates_c
	test -z "$(webserver_usr)" || chown $(webserver_usr) $(DESTDIR)$(phpadmindir)/templates_c
# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
